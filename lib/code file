require 'pp'
require 'pry'
$LOAD_PATH.unshift(File.dirname(__FILE__))
require 'directors_database'

def directors_totals(nds)

          hash_index = 0
          gross_total = 0
          result = {
          }
          
          # while 0 < 9 do 
          while hash_index < nds.length do
            pp nds[hash_index][:name]
            #end
            movie_index = 0
            
            while movie_index < nds[hash_index][:movies].length do 
              # pp nds[hash_index][:movies][movie_index][:worldwide_gross]
              gross_total += nds[hash_index][:movies][movie_index][:worldwide_gross]
              movie_index += 1
            end
            binding.pry
    
                    hash_index += 1
                    result[nds[hash_index][:name]] = gross_total
                    result[nds[hash_index] = gross_total
                    #pp result
                end
end
   
  
    
     
  # Remember, it's always OK to pretty print what you get *in* to make sure
  # that you know what you're starting with!
  #
  #
  # The Hash result be full of things like "Jean-Pierre Jeunet" => "222312123123"
  # result = {
  # }
  #
  # Use loops, variables and the accessing method, [], to loop through the NDS
  # and total up all the
  # ...
  # ...
  # ...
  #
  # Be sure to return the result at the end!


  nil
end